import React from 'react'

const Question_1 = () => {
	return (
		<div>
			<p>
				ля группировки опций внутри тега &lt;select&gt; в HTML используется
				элемент &lt;optgroup&gt;. Это позволяет создавать иерархию и
				группировать различные варианты выбора внутри выпадающего списка.
			</p>
		</div>
	)
}

export default Question_1
